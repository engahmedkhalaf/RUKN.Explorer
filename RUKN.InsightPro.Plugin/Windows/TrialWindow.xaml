<Window x:Class="RUKN.InsightPro.Plugin.Windows.TrialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to RUKN Insight Pro"
        Width="440" SizeToContent="Height"
        Background="#0B0F19"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStyle="SingleBorderWindow"
        FontFamily="Segoe UI"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1F2937"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Cursor="Hand" MouseLeftButtonDown="Website_Click" ToolTip="www.ruknbim.com">
                <TextBlock Text="RUKN Insight Pro" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="Next-Gen BIM Coordination" Foreground="#6B7280" FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>
            <Border Grid.Column="1" Background="#070A13" BorderBrush="#1F2937" BorderThickness="1" CornerRadius="4" Padding="8,4" VerticalAlignment="Center"
                    Cursor="Hand" MouseLeftButtonDown="Website_Click" ToolTip="www.ruknbim.com">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="RUKN" Foreground="White" FontWeight="Bold" FontSize="11"/>
                    <TextBlock Text="BIM" Foreground="#3A86FF" FontWeight="Bold" FontSize="11"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Description Section -->
        <StackPanel Grid.Row="1" Margin="0,20,0,0">
            <TextBlock Text="Start Your Free Trial" Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
            <TextBlock Text="Enter your business email below to register and start your free 14-day fully-featured trial of the RUKN Insight Pro add-in."
                       Foreground="#9CA3AF" FontSize="12.5" TextWrapping="Wrap" LineHeight="18"/>
        </StackPanel>

        <!-- Form Input Section -->
        <StackPanel Grid.Row="2" Margin="0,18,0,0">
            <TextBlock Text="Email Address" Foreground="#9CA3AF" FontSize="11" Margin="0,0,0,6"/>
            <TextBox x:Name="EmailInput" Style="{StaticResource RoundedTextBoxStyle}" Height="32" VerticalContentAlignment="Center"/>
            <Button x:Name="BtnStartTrial" Content="Start Free 14-Day Trial" Style="{StaticResource RoundedButtonStyle}"
                    Background="#2563EB" Foreground="White" BorderThickness="0" Height="36" FontSize="13" FontWeight="Bold"
                    Margin="0,12,0,0" Click="BtnStartTrial_Click"/>
        </StackPanel>

        <!-- Links Section -->
        <Grid Grid.Row="3" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="Already have a key? " Foreground="#9CA3AF" FontSize="11.5"/>
                    <TextBlock Text="Activate License" Foreground="#3A86FF" FontSize="11.5" FontWeight="SemiBold"
                               Cursor="Hand" TextDecorations="Underline" MouseLeftButtonDown="ActivateLicense_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Need a license? " Foreground="#9CA3AF" FontSize="11.5"/>
                    <TextBlock Text="Request via Email" Foreground="#3A86FF" FontSize="11.5" FontWeight="SemiBold"
                               Cursor="Hand" TextDecorations="Underline" MouseLeftButtonDown="RequestEmail_Click"/>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1" x:Name="BtnClose" Content="Close" Style="{StaticResource RoundedButtonStyle}"
                    Background="#1E293B" Foreground="White" Width="70" Height="26" Click="BtnClose_Click" VerticalAlignment="Bottom"/>
        </Grid>
    </Grid>
</Window>
